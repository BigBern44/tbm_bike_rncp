name: dbt_velos
version: "1.0.0"
profile: dbt_velos

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  dbt_velos:
    staging:
      # silver : nettoyage, typage, dédoublonnage — recalculable depuis le bronze
      +materialized: view
      +schema: silver
    marts:
      # gold : agrégats et indicateurs métier requêtés par l'analyse
      +materialized: table
      +schema: gold
